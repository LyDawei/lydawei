---
import AboutLayout from "../layouts/AboutLayout.astro"
---

<AboutLayout title="About David Ly">
  <div class="about-content">
    
    <section class="intro-section animated-section">
      <h2 class="section-title">My Story</h2>
      <div class="intro-card">
        <p>I'm David Ly — a senior full-stack developer who builds pragmatic systems designed to last. I focus on clarity, maintainability, and writing code that actually works when it matters.</p>
        <div class="card-shine"></div>
      </div>
    </section>

    <section class="story-grid animated-section">
      <div class="story-card card-1">
        <h3>The Journey</h3>
        <p>With over a decade of experience across mobile, web, and game development, I care more about shipping thoughtful solutions than chasing clever abstractions.</p>
        <div class="item-glow"></div>
      </div>

      <div class="story-card card-2">
        <h3>Leading the Way</h3>
        <p>You might've seen the handle <span class="highlight">Lydawei</span> floating around. It's more than just a username — it's my Chinese name (李大衛), and also a quiet play on the phrase "lead the way." That's who I aspire to be: someone who leads thoughtfully.</p>
        <div class="item-glow"></div>
      </div>

      <div class="story-card card-3">
        <h3>Real Engineering</h3>
        <p>I don't do traditional whiteboard interviews — not because I can't, but because I believe strong engineering is best demonstrated through real code, sound decisions, and collaborative problem-solving.</p>
        <div class="item-glow"></div>
      </div>

      <div class="story-card card-4">
        <h3>Beyond Code</h3>
        <p>Outside of work, I'm raising two kids, developing a psychological platformer in Godot, and exploring storytelling through art and memory.</p>
        <div class="item-glow"></div>
      </div>
    </section>

    <!-- Tech stack tags -->
    <section class="skills-section animated-section">
      <h2 class="section-title">What I Tinker With</h2>
      <div class="tech-tags">
        <span class="tech-tag">TypeScript</span>
        <span class="tech-tag">React</span>
        <span class="tech-tag">Node.js</span>
        <span class="tech-tag">Astro</span>
        <span class="tech-tag">Godot</span>
        <span class="tech-tag">Python</span>
        <span class="tech-tag">JavaScript</span>
      </div>
    </section>
  </div>
</AboutLayout>

<style>
  .about-content {
    max-width: 800px;
    position: relative;
    z-index: 2;
  }

  /* Animated Sections - staggered content reveal */
  .animated-section {
    margin-bottom: var(--spacing-2xl);
    animation: fadeInUp var(--duration-slow) ease-out forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  .intro-section {
    animation-delay: var(--animation-delay-base); /* 0.1s */
  }

  .story-grid {
    animation-delay: calc(var(--animation-delay-base) * 3); /* 0.3s */
  }

  .skills-section {
    animation-delay: calc(var(--animation-delay-base) * 5); /* 0.5s */
  }

  .section-title {
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-lg);
    font-size: 1.5rem;
    position: relative;
    background: linear-gradient(45deg, var(--color-text-primary), var(--color-primary));
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 4s ease-in-out infinite;
  }

  /* Intro Card */
  .intro-card {
    background: linear-gradient(135deg, #2d2d2d 0%, #1a1a1a 100%);
    padding: var(--spacing-lg);
    border-radius: var(--radius-md);
    border-left: 4px solid #64b5f6;
    color: var(--color-text-secondary);
    position: relative;
    overflow: hidden;
    font-size: 1.1rem;
    line-height: 1.7;
    box-shadow: 0 4px 15px rgba(0,0,0,0.4);
    border: 1px solid #404040;
    transition: all 0.3s ease;
  }

  .intro-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.4);
    border-color: var(--color-primary);
  }

  .intro-card p {
    margin: 0;
  }

  /* Story Grid */
  .story-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--spacing-lg);
    margin-top: 1rem;
  }

  /* Story Cards */
  .story-card {
    background: linear-gradient(135deg, #2d2d2d 0%, #1a1a1a 100%);
    padding: var(--spacing-lg);
    border-radius: var(--radius-md);
    box-shadow: 0 4px 15px rgba(0,0,0,0.4);
    border: 1px solid #404040;
    position: relative;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    transform: translateY(20px);
    opacity: 0;
    animation: slideInUp 0.6s ease-out forwards;
  }

  /* Staggered story card animations */
  .card-1 { animation-delay: calc(var(--animation-delay-base) * 6); } /* 0.6s */
  .card-2 { animation-delay: calc(var(--animation-delay-base) * 7); } /* 0.7s */
  .card-3 { animation-delay: calc(var(--animation-delay-base) * 8); } /* 0.8s */
  .card-4 { animation-delay: calc(var(--animation-delay-base) * 9); } /* 0.9s */

  /* Card hover effects - subtle lift and glow */
  .story-card:hover {
    transform: translateY(-8px) scale(1.02);
    border-color: var(--color-primary);
    box-shadow: var(--shadow-xl), 0 0 20px rgba(100, 181, 246, 0.2);
  }

  /* Ambient glow effect for cards */
  .item-glow {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(100, 181, 246, 0.1) 0%, transparent 70%);
    opacity: 0;
    transition: opacity var(--duration-fast) ease;
    pointer-events: none;
  }

  .story-card:hover .item-glow {
    opacity: 1;
    animation: pulse 2s ease-in-out infinite;
  }

  .story-card h3 {
    margin-bottom: 0.8rem;
    color: var(--color-primary);
    font-size: 1.1rem;
    transition: color 0.3s ease;
  }

  .story-card:hover h3 {
    color: var(--color-primary-light);
  }

  .story-card p {
    color: var(--color-text-muted);
    font-size: 0.95rem;
    line-height: 1.6;
    margin: 0;
    transition: color 0.3s ease;
  }

  .story-card:hover p {
    color: var(--color-text-secondary);
  }

  /* Pulsing highlight text effect */
  .highlight {
    color: var(--color-primary);
    font-weight: 600;
    position: relative;
    animation: glow 2s ease-in-out infinite alternate;
  }

  .card-shine {
    position: absolute;
    top: -50%;
    left: -100%;
    width: 50%;
    height: 200%;
    background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transform: rotate(35deg);
    transition: left 0.6s ease;
  }

  .intro-card:hover .card-shine {
    left: 100%;
  }

  /* Skills Section */
  .skills-section {
    text-align: center;
    background: linear-gradient(135deg, #2d2d2d 0%, #1a1a1a 100%);
    padding: var(--spacing-xl);
    border-radius: var(--radius-md);
    border: 1px solid #404040;
    box-shadow: 0 4px 15px rgba(0,0,0,0.4);
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .skills-section:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.4);
    border-color: var(--color-primary);
  }

  .skills-section::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(100, 181, 246, 0.05) 0%, transparent 70%);
    animation: pulse 4s ease-in-out infinite;
  }

  .tech-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: center;
    margin-top: 1rem;
  }

  .tech-tag {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(100, 181, 246, 0.15);
    border: 1px solid rgba(100, 181, 246, 0.4);
    border-radius: 20px;
    color: var(--color-primary);
    font-size: 0.9rem;
    font-weight: 500;
    white-space: nowrap;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    transition: all 0.3s ease;
  }

  .tech-tag:hover {
    background: rgba(100, 181, 246, 0.25);
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(100, 181, 246, 0.4);
  }
  
  @media (max-width: 768px) {
    .about-content {
      padding: 0;
      max-width: 100%;
    }

    .story-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
      margin-top: 0.5rem;
    }

    .intro-card, .story-card, .skills-section {
      padding: var(--spacing-md);
      margin: 0 0.5rem;
    }

    .section-title {
      font-size: 1.2rem;
      margin-bottom: 1rem;
    }

    .intro-card {
      font-size: 1rem;
      line-height: 1.5;
    }

    .story-card h3 {
      font-size: 1rem;
      margin-bottom: 0.6rem;
    }

    .story-card p {
      font-size: 0.9rem;
      line-height: 1.5;
    }

    /* Skills section mobile optimization */
    .skills-section {
      padding: var(--spacing-lg) 1rem;
    }

    .tech-tag {
      font-size: 0.75rem;
      padding: 0.4rem 0.8rem;
    }
  }

  /* Extra small mobile devices */
  @media (max-width: 480px) {
    .about-content {
      padding: 0;
    }

    .intro-card, .story-card, .skills-section {
      margin: 0 0.25rem;
      padding: 0.8rem;
    }

    .section-title {
      font-size: 1.1rem;
    }

    .story-card h3 {
      font-size: 0.95rem;
    }

    .story-card p, .intro-card p {
      font-size: 0.85rem;
    }

    .tech-tag {
      font-size: 0.7rem;
      padding: 0.35rem 0.7rem;
    }
  }
</style>